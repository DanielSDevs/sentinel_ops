"""Código-fonte do pipeline de ML: dados → features → treino → avaliação → registry.

Os notebooks de `modelos_ml/` e o comando `manage.py treinar_modelos` chamam exatamente
estas funções — é o que garante que o que está no notebook é o que roda em produção.
"""
