# Plataforma web (Django). Para o notebook de análise, veja analytics/requirements.txt.
Django>=5.2,<5.3

# Produção (Azure App Service): servidor WSGI e estáticos servidos pela própria aplicação.
gunicorn>=23.0
whitenoise>=6.9

# Exigidos por `python manage.py importar_dataset`, que lê o LW-DATASET.xlsx.
pandas>=2.2
openpyxl>=3.1
